services:
  bot:
    type: web
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "bash run_render.sh"
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
    disk:
      persistent: true
    autoDeploy: false
